export enum roles {
  admin = 'admin',
  customer = 'customer',
}

export enum flightStatuses {
  departed = 'Departed',
  on_time = 'On Time',
  delayed = 'Delayed',
  arrived = 'Arrived',
  scheduled = 'Scheduled',
  cancelled = 'Cancelled',
}

export enum fareConditions {
  economy = 'Economy',
  comfort = 'Comfort',
  business = 'Business',
}
